# Mã trước khi chuyển sang hướng chỉ đọc

Các file ở đây là patch engine, resolver, Shadow Memory, maps spoofing và test
của bản cũ. Build hiện tại không tham chiếu thư mục này. Test cũ giữ để tham
khảo, cần layout/build cũ để chạy.

Bản sao đầy đủ trước thay đổi nằm trong
`backups/before_readonly_20260909_234155/`. ZIP cũ vẫn ở
`output/cf_aim_speed_x4_zygisk.zip`; nó là bản patch, không phải bản chỉ đọc.
